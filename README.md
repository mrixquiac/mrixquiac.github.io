# MyWebPage
mipagina xdxdxd
Q rollo plebada

<p style="font-family:var(--font-sans);font-size:var(--text-sm);
                    color:var(--color-text-muted);padding:var(--space-8) 0;">
            ⚠️ No se pudo cargar <code>projects.json</code>.<br><br>
            <strong>Importante:</strong> el navegador bloquea <code>fetch()</code>
            cuando abres archivos directamente (<code>file://</code>).<br>
            Usa un servidor local para probar esto. La forma más fácil:<br><br>
            <code>python -m http.server 8000</code><br><br>
            Luego abre <code>http://localhost:8000</code> en tu navegador.